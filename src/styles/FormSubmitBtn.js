const styles = {
  submitBtn: {
    display: 'block',
    width: '215px',
    textTransform: 'none',
    borderRadius: '10px',
    lineHeight: 1,
    padding: (theme) => theme.spacing(2.2, 1.75),
    margin: (theme) => theme.spacing(0, 'auto')
  }
};

export default styles;
